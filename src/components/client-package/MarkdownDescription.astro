---
import { marked } from "marked";

interface Props {
  description?: string;
}

const { description } = Astro.props;

const htmlContent = description ? await marked.parse(description) : "";
---

{
  description && (
    <div class="mt-1 text-gray-800 dark:text-gray-300" set:html={htmlContent} />
  )
}
